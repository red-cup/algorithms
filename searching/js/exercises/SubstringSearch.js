"use strict";

/*
    Challenge:
    Find the substring in the starting string.
    Note** There are many ways to complete this challenge. 

    EXAMPLE:
    SubstringSearch("cat", "at") => 1
    SubstringSearch("doggo woggo", "og") => 2
*/

const startingString = "lorie loled";

const SubstringSearch = (startingString, substring) => {
  const numOfMatches = 0;

  return numOfMatches;
};

console.log(SubstringSearch(string, "lo")); // => 2
